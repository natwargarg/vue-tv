export const SET_SHOWS = (state, shows) => {
    state.shows = shows;
}

export const SET_SHOW = (state, show) => {
    state.show = show;
}

export const SET_SEARCH_QUERY = (state, searchQuery) => {
    state.searchQuery = searchQuery;
}


